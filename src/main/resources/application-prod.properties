# === Production config (Supabase PostgreSQL) ===
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.ppsirmzztisotcpsfure
spring.datasource.password=${DB_PASSWORD}

# Don't auto-modify tables (we created them manually in Supabase)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Disable H2 console in production
spring.h2.console.enabled=false

# Server port (Render sets PORT env var)
server.port=${PORT:8080}

# WeChat Work config (sensitive values via environment variables)
wechat.corp-id=ww7d266fc9ec52eb66
wechat.app-secret=${WECHAT_SECRET}
wechat.agent-id=1000008
